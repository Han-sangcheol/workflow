[pytest]
# pytest 설정 파일
# 본 프로젝트는 가상환경(venv)에서 테스트를 실행합니다

testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# 로그 출력 설정
log_cli = true
log_cli_level = INFO

# 경고 필터
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning

