"""
AI 프롬프트 설정
업무일지 분석을 위한 프롬프트 템플릿을 정의합니다.
"""

from typing import Dict


# 시스템 프롬프트 (AI 역할 정의)
SYSTEM_PROMPT = """당신은 FW팀의 업무일지를 분석하고 정리하는 전문가입니다.
정확하고 체계적으로 업무 내용을 정리하며, 항상 한국어로 응답합니다.
중국어 간체자는 절대 사용하지 않습니다."""


# 텍스트 정리 프롬프트 (1단계: 원본 텍스트 → 정리된 텍스트)
# 목적: PDF에서 추출한 원본 텍스트의 구조를 정리 (내용 삭제/단축 금지)
CLEANING_PROMPT = """다음은 여러 팀원의 일일 업무일지 원본 텍스트입니다.
PDF에서 추출되어 구조가 깨져있으므로 읽기 쉽게 정리해주세요.

[핵심 규칙]
1. 원본 내용 100% 유지 (삭제/요약 금지)
2. 각 팀원의 업무는 해당 팀원에게만 작성 (혼동 금지)
3. "=== 파일: ...FW팀 [이름]... ===" 구분자로 팀원 구분

[팀원 구분 방법]
- "=== 파일: ...FW팀 [이름]... ===" 이후 내용 → 해당 팀원의 업무
- 다음 "=== 파일:" 이 나오기 전까지가 해당 팀원의 업무입니다

[출력 형식]
########## 팀원 목록 ##########
[이름1], [이름2], ...

########## [이름1] (YYYY.MM.DD) ##########
【금일업무】
• 프로젝트: [프로젝트명]
  - 목적: [내용]
  - Action: [내용]
  - 진행률: [계획]% / [달성]%

【익일업무】
• 프로젝트: [프로젝트명]
  - [내용]

########## [이름2] (YYYY.MM.DD) ##########
(위와 동일한 형식으로 해당 팀원의 업무만 작성)

[원본 텍스트]
{documents_text}
"""


# 통합 회의록 생성 프롬프트 (2단계: 정리된 텍스트 → 회의록)
SUMMARY_PROMPT = """다음은 정리된 팀원들의 일일 업무일지입니다.
이 내용을 바탕으로 통합 회의록을 작성해주세요.

[!!! 가장 중요 - 데이터 출처 규칙 !!!]
★★★ 회의록의 모든 내용은 반드시 아래 [정리된 업무일지]에서만 추출하세요 ★★★
- 양식의 예시 텍스트(예: "[세부 기능]", "[상세 업무 내용]")를 절대 그대로 쓰지 마세요
- 양식은 구조/형식만 참고하고, 내용은 100% 원본 데이터에서 가져오세요
- 원본 데이터에 없는 내용은 작성하지 마세요

[핵심 지침]
1. **참석자**: 정리된 텍스트 상단의 "팀원 목록"에서 모든 이름을 추출하여 기재
2. **프로젝트 대분류 기준 통합**: 
   - 같은 프로젝트 대분류명은 하나의 항목으로 통합
   - 예: "[프로젝트 대분류: A]" → "1 A 개발"로 통합
3. **같은 프로젝트 대분류는 반드시 하나로**: 여러 팀원의 같은 프로젝트 업무를 하나로 합침
4. **담당자/목표일자**: 각 프로젝트 맨 마지막에 한 번만 기재
5. **진행률**: 해당 프로젝트의 평균 또는 대표 진행률 표시

[회의록 구조 (구조만 참고, 내용은 원본 데이터 사용)]
================================================================================
                              회   의   록
--------------------------------------------------------------------------------
회의주제: 진료장비사업부 펌웨어팀 일일 회의
회의일자: (원본 데이터의 날짜)
회의장소: 5층 진료장비사업부
참 석 자: (팀원 목록에서 추출한 실제 이름들)
================================================================================

■ 회의내용
--------------------------------------------------------------------------------
금주(일일) 이슈 사항

1 (프로젝트명 - 원본 데이터에서 추출) (실제 진행률%)
  1.1 (원본 데이터의 실제 세부 기능)
      a (원본 데이터의 실제 업무 내용)
      b (원본 데이터의 실제 업무 내용)
  목표일자: (원본 데이터의 실제 날짜)
  담당자: (해당 프로젝트 참여 팀원 실제 이름들)

                          - 이상입니다 -
================================================================================

[작성 규칙]
1. 프로젝트 통합:
   - "프로젝트 대분류"가 같으면 무조건 하나의 프로젝트로 통합
   - 팀원별로 분리하지 않고, 프로젝트별로 모든 업무를 합침
   - 세부 기능, 업무 내용에서 팀원 이름 및 개별 진행율 제거
   
2. 번호 체계:
   - 대분류 프로젝트: 1, 2, 3, 4, 5... (모든 프로젝트 포함, 수 제한 없음)
   - 세부 기능: 1.1, 1.2, 1.3... 
   - 상세 업무: a, b, c, d...

3. 프로젝트 누락 금지:
   - 업무일지에 언급된 모든 프로젝트를 빠짐없이 포함
   - 프로젝트를 임의로 생략하거나 과도하게 병합하지 않음
   - 유사한 이름이라도 다른 프로젝트는 별도 항목으로 유지

4. 담당자:
   - 해당 대분류 프로젝트에 업무가 있는 모든 팀원 이름 나열
   - 예: "담당자: 홍길동, 김철수, 이영희"

5. 참석자:
   - 반드시 팀원 목록의 모든 이름을 기재 (누락 금지)

[정리된 업무일지 - 이 데이터에서만 내용 추출]
{cleaned_text}
"""


# 감사 인사 생성 프롬프트 (3단계: 정리된 텍스트 → 감사 인사)
THANKS_PROMPT = """다음은 정리된 팀원들의 일일 업무일지입니다.
각 팀원의 업무 내용을 바탕으로 개인별 감사 인사를 작성해주세요.

[요구사항]
1. 각 팀원의 구체적인 업무 내용을 언급하여 개인화된 메시지 작성
2. 부하직원에게 하는 말로 너무 높임말로 하지 않아도 됩니다.
3. 각 팀원당 1-2문장 정도로 작성
4. 따뜻하고 격려하는 톤 유지
5. 반드시 한국어로만 작성 (중국어 간체자 사용 금지)
6. "주셔서"->"주어서" 으로 변경
7. "주셨습니다"-> "주었습니다" 으로 변경
8. 출력물 이름옆 "**" 제거

[감사 인사 형식]
## 팀원별 감사 인사

1 [이름]씨에게 감사합니다.
[개인화된 감사 메시지 - 업무 내용 언급]

2 [이름]씨에게 감사합니다.
[개인화된 감사 메시지 - 업무 내용 언급]

3...
[정리된 업무일지]
{cleaned_text}
"""


# 기간별 성과 분석 프롬프트
PERIOD_ANALYSIS_PROMPT = """다음은 {period_info} 기간 동안의 팀원별 업무 기록입니다.
이 데이터를 분석하여 기간별 성과 보고서를 작성해주세요.

[분석 항목]
1. 주요 성과 요약
   - 기간 내 완료된 주요 업무
   - 진행 중인 핵심 프로젝트 현황
   
2. 팀원별 기여도 분석
   - 각 팀원의 주요 업무 및 성과
   - 업무 집중도가 높은 영역
   
3. 프로젝트별 진행 현황
   - 프로젝트별 진행률 및 상태
   - 지연되거나 주의가 필요한 항목
   
4. 향후 계획 및 제언
   - 다음 기간에 집중해야 할 업무
   - 개선이 필요한 부분
   - 우선순위 높은 작업 추천

[출력 형식]
================================================================================
                    기간별 성과 분석 보고서
                    {period_info}
================================================================================

■ 1. 주요 성과 요약
--------------------------------------------------------------------------------
[완료된 주요 업무 목록]
[진행 중인 핵심 프로젝트]

■ 2. 팀원별 기여도
--------------------------------------------------------------------------------
[팀원1]
- 주요 업무:
- 성과:

[팀원2]
...

■ 3. 프로젝트별 현황
--------------------------------------------------------------------------------
[프로젝트1] (진행률%)
- 상태:
- 주요 진척:

■ 4. 향후 계획 및 제언
--------------------------------------------------------------------------------
[다음 기간 집중 업무]
[개선 필요 사항]
[우선순위 작업 추천]

================================================================================

[업무 데이터]
{tasks_text}
"""


# 개발 현황 생성 프롬프트 (4단계: 정리된 텍스트 → 개발 현황)
DEVSTATUS_PROMPT = """다음은 정리된 팀원들의 일일 업무일지입니다.
이 내용을 바탕으로 오전/오후 개발 현황 공유 메시지를 작성해주세요.

[!!! 핵심 규칙 - 반드시 준수 !!!]
★★★ 업무일지에 언급된 모든 프로젝트를 빠짐없이 포함하세요 ★★★
- 프로젝트를 임의로 생략하거나 병합하지 마세요
- 각 프로젝트의 주요 업무 내용을 모두 나열하세요
- 동일 프로젝트의 유사 업무는 같은 프로젝트 아래 통합

[작성 규칙]
1. 업무일지에서 프로젝트명을 먼저 모두 추출
2. 각 프로젝트별로 해당하는 모든 업무 내용 정리
3. 번호 체계: 대분류(1, 2), 소분류(1.1, 1.2) 형식
4. 반드시 한국어로만 작성
5. 담당자 이름 생략 가능

[프로젝트 식별 기준]
- "프로젝트:" 다음에 오는 이름이 프로젝트명
- 유사한 프로젝트명은 통합 (예: "bright Simple 4구", "bright simple 완제품" → "bright Simple")
- 완전히 다른 프로젝트는 별도로 분리

[출력 형식]
================================================================================
                         ☀️ 개발현황
================================================================================

안녕하세요 진료장비사업부 펌웨어팀
오전/오후 개발현황 팀 내 공유 드립니다	

1 [프로젝트명1]
  1.1 [업무 내용]
  1.2 [업무 내용]

2 [프로젝트명2]
  2.1 [업무 내용]
  2.2 [업무 내용]

3 [프로젝트명3]
  3.1 [업무 내용]

(프로젝트가 더 있으면 계속 추가)

================================================================================

[정리된 업무일지]
{cleaned_text}
"""


# 프로젝트 추천 프롬프트
PROJECT_RECOMMEND_PROMPT = """다음은 현재 진행 중인 프로젝트들의 상태입니다.
이 데이터를 분석하여 다음 할 일과 우선순위를 추천해주세요.

[분석 항목]
1. 프로젝트별 상태 평가
   - 진행률 대비 목표일 분석
   - 일정 위험도 평가 (정상/주의/위험)
   
2. 우선순위 조정 제안
   - 현재 우선순위의 적절성 평가
   - 조정이 필요한 경우 근거와 함께 제안
   
3. 다음에 해야 할 작업 추천
   - 프로젝트별 구체적인 다음 단계
   - 예상 소요 시간
   
4. 위험 요소 및 대응 방안
   - 지연 가능성이 있는 프로젝트
   - 리소스 부족 예상 영역
   - 대응 전략 제안

[출력 형식]
================================================================================
                    📊 프로젝트 분석 및 추천
================================================================================

■ 1. 프로젝트별 상태 평가
--------------------------------------------------------------------------------
[프로젝트1] - 상태: 🟢정상 / 🟡주의 / 🔴위험
• 현재 진행률: X% (목표 대비 +/-Y%)
• 일정 분석: 
• 평가:

■ 2. 우선순위 조정 제안
--------------------------------------------------------------------------------
현재 우선순위 → 제안 우선순위
[프로젝트명]: 보통 → 높음 (사유: ...)

■ 3. 다음 할 일 추천
--------------------------------------------------------------------------------
🔥 긴급 (이번 주 완료 필요)
1. [프로젝트] - [구체적인 작업] (예상 N시간)

📌 중요 (이번 달 내)
1. [프로젝트] - [구체적인 작업]

📋 일반
1. [프로젝트] - [구체적인 작업]

■ 4. 위험 요소 및 대응 방안
--------------------------------------------------------------------------------
⚠️ [위험 요소 1]
• 영향 프로젝트:
• 대응 방안:

================================================================================

[프로젝트 현황 데이터]
{project_data}
"""


# === 프롬프트 조회 함수 ===

def get_prompt(prompt_type: str, include_examples: bool = True) -> str:
    """
    프롬프트 반환 (사용자 설정 우선, 없으면 기본값)
    예제가 등록되어 있으면 {examples} 플레이스홀더를 예제로 대체
    
    Args:
        prompt_type: "cleaning", "summary", "thanks", "devstatus" 중 하나
        include_examples: True면 {examples} 플레이스홀더를 예제로 대체
        
    Returns:
        프롬프트 문자열 (예제 포함)
    """
    # 순환 임포트 방지를 위해 함수 내에서 임포트
    from ..utils.settings_manager import get_settings
    
    settings = get_settings()
    
    # 기본 프롬프트 매핑
    default_prompts = {
        "cleaning": CLEANING_PROMPT,
        "summary": SUMMARY_PROMPT,
        "thanks": THANKS_PROMPT,
        "devstatus": DEVSTATUS_PROMPT,
        "period_analysis": PERIOD_ANALYSIS_PROMPT,
        "project_recommend": PROJECT_RECOMMEND_PROMPT,
    }
    
    # 사용자 프롬프트 조회
    user_prompts = settings.get_all_prompts()
    user_prompt = user_prompts.get(prompt_type, "")
    
    # 사용자 프롬프트가 있으면 사용, 없으면 기본값
    if user_prompt and user_prompt.strip():
        prompt = user_prompt
    else:
        prompt = default_prompts.get(prompt_type, "")
    
    # 예제 참조 처리
    if include_examples and "{examples}" in prompt:
        user_examples = settings.get_all_examples()
        example = user_examples.get(prompt_type, "")
        
        if example and example.strip():
            # 예제가 있으면 플레이스홀더 대체
            prompt = prompt.replace("{examples}", example)
        else:
            # 예제가 없으면 플레이스홀더 제거
            prompt = prompt.replace("{examples}", "")
    
    return prompt


def get_example(prompt_type: str) -> str:
    """
    예제 반환
    
    Args:
        prompt_type: "cleaning", "summary", "thanks", "devstatus" 중 하나
        
    Returns:
        예제 문자열 (없으면 빈 문자열)
    """
    from ..utils.settings_manager import get_settings
    
    settings = get_settings()
    user_examples = settings.get_all_examples()
    return user_examples.get(prompt_type, "")


def get_default_prompt(prompt_type: str) -> str:
    """
    기본 프롬프트 반환 (항상 코드에 정의된 기본값)
    
    Args:
        prompt_type: "cleaning", "summary", "thanks", "devstatus", "period_analysis", "project_recommend" 중 하나
        
    Returns:
        기본 프롬프트 문자열
    """
    default_prompts = {
        "cleaning": CLEANING_PROMPT,
        "summary": SUMMARY_PROMPT,
        "thanks": THANKS_PROMPT,
        "devstatus": DEVSTATUS_PROMPT,
        "period_analysis": PERIOD_ANALYSIS_PROMPT,
        "project_recommend": PROJECT_RECOMMEND_PROMPT,
    }
    return default_prompts.get(prompt_type, "")


def get_all_default_prompts() -> Dict[str, str]:
    """모든 기본 프롬프트 반환"""
    return {
        "cleaning": CLEANING_PROMPT,
        "summary": SUMMARY_PROMPT,
        "thanks": THANKS_PROMPT,
        "devstatus": DEVSTATUS_PROMPT,
        "period_analysis": PERIOD_ANALYSIS_PROMPT,
        "project_recommend": PROJECT_RECOMMEND_PROMPT,
    }

